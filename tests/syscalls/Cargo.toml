[package]
authors = [
    "Grzegorz Płaczek <gplaczek@internships.antmicro.com>"
]
name = "syscalls_test"
version = "0.1.0"

[dependencies]
wasi = "0.11.0"
wasi_ext_lib = { git = "https://github.com/antmicro/wasm_ext_lib.git", branch = "master" }

[build]
target = "wasm32-wasi"
